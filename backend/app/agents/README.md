# Agent Layer

Planned agents:

- intent agent: understand guest requests
- policy agent: retrieve and apply SOP rules
- response agent: generate guest communication

The first version keeps business logic deterministic. LLM reasoning will be introduced after workflow validation.
